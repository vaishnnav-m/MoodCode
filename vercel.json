{
  "buildCommand": "npm install && npm run compile -w @moodcode/shared && npm run build -w dashboard",
  "outputDirectory": "dashboard/dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}