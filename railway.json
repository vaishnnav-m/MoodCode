{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run compile -w @moodcode/shared && npm run build -w backend"
  },
  "deploy": {
    "startCommand": "node backend/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}